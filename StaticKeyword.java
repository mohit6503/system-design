public class StaticKeyword {
    public static void main(String[] args) {
        
    }
}


// static keyword in java is used to share the same variable or method of a given class. 
// It can be used with variables, methods, blocks and nested classes. The static keyword belongs to the 
// class rather than any instance of the class. This means

// non static members can be accessed only through objects, while static members can be accessed directly using the class name.

// static member share the memory once 

// static member can be access without creating object of the class. It can be accessed directly using class name and dot operator;


